# Repository Instructions

- When creating or modifying skills, you must invoke Skill Creator first.
- Develop directly on the local `main` branch by default.
- Do not create feature branches or worktrees unless the user explicitly asks.
- When pushing, always push `main` with `--force-with-lease`, for example:
  `git push --force-with-lease origin main`
